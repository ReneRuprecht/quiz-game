/// creates a request object for the requests that needs an username and game id.
class GameIdUsernameRequest{
  String username;
  String gameId;

  GameIdUsernameRequest(this.username, this.gameId);
}