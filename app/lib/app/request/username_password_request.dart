/// creates a request object for the requests that needs an username and password.
class UsernamePasswordRequest {
  String username;
  String password;

  UsernamePasswordRequest({required this.username, required this.password});
}
